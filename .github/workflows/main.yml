name: CI

on: [push]


jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: |
        ls -l
        echo 'down compose'
        docker-compose down
        echo 'Runing docker-compose'
        docker-compose up
        echo 'Done'
        
